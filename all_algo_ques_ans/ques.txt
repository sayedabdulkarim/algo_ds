1 - twoSum - https://www.youtube.com/watch?v=J0U1obg5BEM&list=PLMPgoZdlPumdjYOZ2WO4zTdvH0uxEZ8ii&index=17
2 - fibonacci
3 - factorial
4 - primeNum
5 - isPower2
6 - stack implementation
7 - Queue implementation ( arr way, object way )
8 - LinkedList implementation
9 - Best Time to Buy and Sell Stock - https://www.youtube.com/watch?v=ipClF88Tkks
10 - unique array of numbers nd objects
11 - inventory list function which returns 3 methods ( add, remove, getList )
12 = find element index in an array ( using binarySearch )
13 - sort array ( bubble sort ) - https://www.youtube.com/watch?v=xdCgW2a3r_Q&list=PLC3y8-rFHvwiRYB4-HHKHblh3_bQNJTMa&index=21









//// learn space complexity


///
function inventoryList() {
  // write your code here
   this.inventoryItems = [];
  
  const checkUnique = str => this.inventoryItems.find( i => i.itemName == str )
  
  this.add = (str) => {
      if(!checkUnique(str)){

        const obj  = {
              id: Math.random(),
              itemName: str
          }

        this.inventoryItems.push(obj)
      } else {
          console.log("existed")
      }
         
  }
  
  this.remove = str => {
      this.inventoryItems = this.inventoryItems?.filter( item => item.itemName != str ); 
  }
  
  this.getList = () => {
      this.inventoryItems?.forEach( item => console.log(item)) 
  }
  
}